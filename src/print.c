#include "interfaces.h"
#include "read.h"
#include "print.h"
#include <stdio.h>


void stampa(char *str, table *t) {
    int i = 0;
    int len = 0;
    int *arr = load(&(*str), &len);



    if(arr != NULL) {
        for(i=0; i<len; i++) {

        }

    }

}
